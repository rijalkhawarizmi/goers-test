name: Deploy Android App to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:
    branches:
      - development
    inputs:
      version:
        description: "App Version (e.g., 1.0.0)"
        required: true
        default: "1.0.0"
      build_number:
        description: "Build Number (e.g., 42)"
        required: true
        default: "1"
      release_notes:
        description: "Release Notes"
        required: true
        default: "Production Release"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v1
        with:
          java-version: "17"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.24.5"
          channel: "stable"
          cache: true

      - name: Setup Ruby and Fastlane
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true
          working-directory: android

      - name: Configure Keystore
        run: |
          echo "$RELEASE_SIGNING_KEY" > release.jks.b64
          base64 -d -i release.jks.b64 > android/app/release.keystore

      - name: Set up Supply Key file
        working-directory: android
        run: echo "$SUPPLY_KEY" > fastlane/supply.json

      - name: Setup store key properties
        working-directory: android
        run: echo "$RELEASE_KEYSTORE_PROPERTIES" > key.properties

      - name: Ensure Fastlane Metadata Directory Exists
        working-directory: android
        run: mkdir -p fastlane/metadata/android/en-US/changelogs

      - name: Build and Deploy to Play Store
        working-directory: android
        run: |
          bundle exec fastlane android production \
            version:${{ github.event.inputs.version }} \
            build_number:${{ github.event.inputs.build_number }} \
            release_notes:"${{ github.event.inputs.release_notes }}"
=======
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run development-specific tests
        run: echo "Running tests for development branch..."
      # Add other steps relevant to your development workflow
>>>>>>> 0f18d11eed94616465562d1d37a066de89158391
